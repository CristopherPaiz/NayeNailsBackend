import express from 'express'

// import { deleteUser } from '../controllers/admin.js' // deleteUser no se usa en este archivo.
// Las siguientes funciones no están importadas: getAllUsers, getUserById
// Asumo que deberían estar en '../controllers/admin.js' o similar.

const router = express.Router()

// Rutas para la gestión de usuarios
// router.get('/users', getAllUsers) <--- getAllUsers no definido
// router.get('/users/:id', getUserById) <--- getUserById no definido

export default router
